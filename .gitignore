# Third-party tools — build separately, don't commit
llama.cpp/

# Model weights — too large for git; distribute separately (see README)
*.gguf
model/

# Python
venv/
__pycache__/
*.pyc

# OS
.DS_Store

# Secrets — NEVER commit
.env
.streamlit/secrets.toml

# Runtime artifacts and legacy code
logs/
old_scrapers/
